using System;

namespace Rozdzial22_zad5_LINQ_QUERY
{
    class Program
    {
        static void Main(string[] args)
        {
            //5.By using the extension methods OrderBy(…) and ThenBy(…) with lambda expression,
            //sort a list of students by their first and last name in descending order.
            //Rewrite the same functionality using a LINQ query.

            // Rozwiazanie z zad 3 lub zad 4



            Console.ReadKey();
        }
    }
}
